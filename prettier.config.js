// @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  tabWidth: 2,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 80,
  arrowParens: 'avoid',
}

export default config
